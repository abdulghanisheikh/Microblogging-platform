<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
        <div class="navbar w-full flex flex-col justify-between py-3 gap-3">
            <div class="flex justify-between items-center py-2 px-10">
                <h1 class="text-4xl tracking-tighter"><%= user.username %></h1>
                <div class="flex gap-5">
                    <a href="/" class="text-white bg-zinc-800 hover:bg-zinc-200 hover:text-black duration-[0.75s] ease-in-out py-3 px-4 border-1 outline-none rounded-lg border-zinc-800 text-xl tracking-tighter">Home</a>
                    <a href="/create" class="text-green-500 bg-zinc-800 hover:bg-green-500 hover:text-white duration-[0.75s] ease-in-out py-3 px-4 border-1 outline-none rounded-lg border-zinc-800 text-xl tracking-tighter">New User</a>
                    <a href="/logout" class="text-red-500 bg-zinc-800 hover:bg-red-500 hover:text-white duration-[0.75s] ease-in-out py-3 px-4 border-1 outline-none rounded-lg border-zinc-800 text-xl tracking-tighter">Logout</a>
                </div>
            </div>
            <hr class="w-full border-zinc-800">
        </div>
        <div class="flex gap-5 w-full px-10 justify-around py-5">
            <div class="profile flex flex-col gap-10 w-1/2">
                <div class="updiv flex gap-5 items-center">
                    <div class="h-35 w-32 overflow-hidden rounded-lg border-1 bg-zinc-800 cursor-pointer">
                    <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilePic %>" alt="">
                    </div>
                    <div class="flex flex-col justify-between items-between h-35">
                        <div class="h-5 w-5 mt-3 rounded-md overflow-hidden bg-zinc-300 p-1"><a href="/profile/upload">
                            <img class="h-fit w-fit object-cover" src="/images/edit.png" alt="">
                        </a></div>
                        <h1 class="text-4xl tracking-tighter self-end"><span class="text-zinc-600 font-light">Hello,</span> <%= user.name %> 👋</h1>
                    </div>
                </div>
                <div class="mt-5 downdiv">
                    <h3 class="text-2xl">You can create a new post</h3>
                    <form action="/post" method="post">
                        <textarea type="text" class="mt-2 border-1 outline-none rounded-lg p-3 resize-none w-100 h-30" placeholder="What's on your mind?" name="content"></textarea>
                        <input type="submit" class="bg-blue-600 rounded-lg p-3 outline-none block mt-2 px-4 cursor-pointer hover:bg-blue-500 duration-300 ease-in-out active:scale-95" value="Create Post">
                    </form>
                </div>
            </div>
            <div class="posts w-1/2 py-5 text-zinc-400">
                <h1 class="text-zinc-500 tracking-tighter mb-5 text-xl">Your posts</h1>
                <div class="postContainer flex flex-wrap items-end gap-5">
                    <% user.post.reverse().forEach(function(item){ %>
                        <div class="post hover:scale-105 duration-300 ease-in-out flex flex-col items-between w-100 rounded-xl border-[1px] p-3 outline-none bg-zinc-800 border-zinc-600">
                            <div>
                                <h1 class="text-blue-500">@<%= user.username %></h1>
                                <p class="text-sm tracking-tight"><%= item.content %></p>
                            </div>
                            <div class="mt-5">
                                <small><%= item.like.length %></small>
                                <div class="btns flex justify-between items-center">
                                    <a href="/like/<%= item._id %>" class="text-zinc-400 text-shadow-like">
                                        <% if(item.like.indexOf(user._id)===-1){ %>
                                            Like
                                        <% } else{ %>
                                            Unlike
                                        <% } %>
                                    </a>
                                    <div class="flex gap-3">
                                        <a href="/delete/<%= item._id %>" class="text-shadow-delete text-red-500">Delete</a>
                                        <a href="/edit/<%= item._id %>" class="text-shadow-edit text-zinc-400">Edit</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>