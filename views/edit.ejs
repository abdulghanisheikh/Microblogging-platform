<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
</head>
<body>
    <div class="w-full flex flex-col py-5 gap-10 min-h-screen bg-zinc-900 text-white">
        <div class="navbar flex flex-col gap-5 justify-between w-full">
            <div class="flex gap-5 px-20 self-end">
                <a href="/profile" class="text-blue-500 bg-zinc-800 hover:bg-blue-500 hover:text-white duration-[0.75s] ease-in-out py-3 px-4 border-1 outline-none rounded-lg border-zinc-800 text-xl tracking-tighter">Back to Profile</a>
            </div>
            <hr class="w-full border-zinc-800">
        </div>
        <div class="flex flex-col gap-5 px-25">
            <h1 class="text-5xl tracking-tight createLine">Update User.</h1>
            <form action="/edit/<%= post._id %>" method="post">
                <textarea type="text" class="mt-2 border-1 outline-none rounded-lg p-3 resize-none w-100 h-30" placeholder="What's on your mind?" name="content"></textarea>
                <input type="submit" class="bg-blue-600 rounded-lg p-3 outline-none block mt-2 px-4 cursor-pointer hover:bg-blue-500 duration-300 ease-in-out active:scale-95" value="Update">
            </form>
        </div>
    </div>
</body>
</html>